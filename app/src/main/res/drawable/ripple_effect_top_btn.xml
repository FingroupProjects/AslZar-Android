<?xml version="1.0" encoding="utf-8"?>
<ripple
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/background_3"> <!-- Ripple color :: WHITE -->

    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/background_2"/>
            <corners android:topLeftRadius="18dp"/>
        </shape>
    </item>

    <!-- Make sure the ripple doesn't exceed the bounds -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners android:topLeftRadius="18dp"/>
            <solid android:color="@color/background_3" />
        </shape>
    </item>
</ripple>